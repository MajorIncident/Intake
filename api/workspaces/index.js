/** @module api/workspaces/index @description Vercel endpoint that creates collaboration workspaces. */
import { createWorkspaceHandler } from '../_workspace.js';
export default createWorkspaceHandler();
