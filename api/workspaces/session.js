/**
 * @module api/workspaces/session
 * @description Stable Vercel endpoint that loads and conditionally updates the bearer-token workspace.
 */
import { workspaceHandler } from '../_workspace.js';

export default workspaceHandler();
