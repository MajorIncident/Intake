/** @module api/workspaces/token @description Vercel endpoint that loads and conditionally updates one secret-link workspace. */
import { workspaceHandler } from '../_workspace.js';
export default workspaceHandler();
